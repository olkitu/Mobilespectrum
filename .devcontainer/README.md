# Devcontainer

## Start development server

Start development server in devcontainer with command `ng serve` with `--host` argument to listen `0.0.0.0` to connect outside from devcontainer environment.

```bash
ng serve --host 0.0.0.0
```